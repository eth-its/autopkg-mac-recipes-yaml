Description: Creates a Self-Service Policy available to members of a Testing group.
Identifier: com.github.eth-its-recipes.jamf.XQuartz-nodes
ParentRecipe: com.github.eth-its-recipes.jamf.template-OS-exclude-min-script-nodes
MinimumVersion: "2.3"

Input:
  NAME: XQuartz
  UNTESTED_RECIPE_IDENTIFIER: local.jamf.XQuartz
  OS_EXCLUDE_MIN: "28"
  OS_EXCLUDE_MIN_GROUP_NAME: "macOS %OS_EXCLUDE_MIN% or higher"
  OS_EXCLUDE_MIN_GROUP_TEMPLATE: SmartGroup-OS-exclude-min.xml  
  SP_PROCESS_NAME: X11
  SP_EXECUTABLE_COMMAND: open '/Applications/Utilities/XQuartz.app'
  SCRIPT_NAME: XQuartz-postinstall.sh
  SCRIPT_PATH: XQuartz-postinstall.sh
  SCRIPT_PRIORITY: After