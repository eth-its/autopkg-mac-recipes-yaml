Description: |
  Uploads a script to the Jamf Pro Server and creates a Self Service Policy.

Identifier: com.github.eth-its-recipes.jamf.ETHPrinters-reset-printer-credentials
MinimumVersion: "2.3"

Input:
  NAME: ETH Printers - Reset Printer Credentials
  POLICY_CATEGORY: ETH
  TEST_USERS_GROUP_NAME: Testing
  TESTING_GROUP_TEMPLATE: SmartGroup-test-users.xml
  POLICY_NAME: "%NAME%"

  SELFSERVICE_POLICY_CATEGORY: ETH
  SELFSERVICE_POLICY_TEMPLATE: Policy-prod-selfservice-install.xml
  SELFSERVICE_DISPLAY_NAME: "%NAME%"
  SELFSERVICE_DESCRIPTION: Resets all ETH printing credentials. Send a new print job to specify new credentials.
  SELFSERVICE_ICON: "ETH Printers.png"
  SELFSERVICE_POLICY_NAME: "%NAME%"
  INSTALL_BUTTON_TEXT: Reset Credentials
  REINSTALL_BUTTON_TEXT: Reset Credentials
  FORCE_DESCRIPTION: "false"

  SCRIPT_NAME: ETHPrinters-CredentialReset.sh
  SCRIPT_PRIORITY: After


Process:
  - Processor: com.github.grahampugh.jamf-upload.processors/JamfCategoryUploader
    Arguments:
      category_name: "%SELFSERVICE_POLICY_CATEGORY%"


  - Processor: com.github.grahampugh.jamf-upload.processors/JamfScriptUploader
    Arguments:
      script_category: "%POLICY_CATEGORY%"
      script_path: "%SCRIPT_NAME%"
      replace_script: "True"

  - Processor: com.github.grahampugh.jamf-upload.processors/JamfComputerGroupUploader
    Arguments:
      computergroup_template: "%TESTING_GROUP_TEMPLATE%"
      computergroup_name: "%TESTING_GROUP_NAME%"
      replace_group: "False"

  - Processor: com.github.grahampugh.jamf-upload.processors/JamfPolicyUploader
    Comment: Self Service install policy
    Arguments:
      policy_template: "%SELFSERVICE_POLICY_TEMPLATE%"
      policy_name: "%SELFSERVICE_POLICY_NAME%"
      icon: "%SELFSERVICE_ICON%"
      USERS_GROUP_NAME: "%TESTING_GROUP_NAME%"
      replace_policy: "True"